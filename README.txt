1- There is no primary key for faculty table.
2- In the attribute degree1, degree2, degree3 the Record is saved using (,) between each information,
In ex (MS in Comuter Science, 2018, AUB) which is incorrect.
4- Records should not saved to the tuples degree directly, it will increase null values if some instructors had one degree,
so the other two will be null.
